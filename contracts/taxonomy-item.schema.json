{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://database.locobox.ch/schemas/taxonomy-item.schema.json",
  "title": "Locobox Taxonomy Item",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "name", "slug"],
  "properties": {
    "schemaVersion": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "slug": {
      "type": "string",
      "description": "URL- und dateisystemfreundlicher Slug; Dateiname ist slug in Kleinbuchstaben (z. B. H0 in tags/h0.json).",
      "pattern": "^[A-Za-z0-9]+(?:-[A-Za-z0-9]+)*$"
    },
    "description": {
      "type": "string"
    }
  }
}
